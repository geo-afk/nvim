return {
  settings = {
    typos_lsp = {
      -- config = vim.fn.expand '~/AppData/Local/nvim/typos.toml',
      -- How typos are rendered in the editor, can be one of an Error, Warning, Info or Hint.
      -- Defaults to error.
      -- diagnosticSeverity = 'Hint', -- was "Error"
      diagnosticSeverity = 'Hint', -- was "Error"
    },
  },
}
