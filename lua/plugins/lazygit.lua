-- =============================================================================
--  plugins/lazygit.lua  ·  Floating LazyGit terminal
--
--  Uses the local custom/float_term module (no external plugin needed).
--  LazyGit must be installed on your system: https://github.com/jesseduffield/lazygit
-- =============================================================================

-- No vim.pack entry – this uses the built-in :terminal + a local Lua module.
